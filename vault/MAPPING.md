# What each component is held against

Generated by `vault/map.py` from `vault/MANIFEST.json`. Joining a reference to a
component is the boring half: the measured timings of the files in play, and an
explicit statement of what the vault cannot yet show. Reading *how* a reference moves
is a person's job, done against `vault/sheet.py VERIFIED=1` output and written down in
`vault/READINGS.json`; it is reproduced per component below, and this file will not be
written at all while a quoted file lacks one.

What the readings agree on, across every reference an eye has passed: the frame, the
labels and the track are complete on frame zero and never move; exactly one ink changes,
and it changes because a measurement changed; a loop is a rolling window rather than a
rewind; and a dead state is drawn as absence — an empty lane, a blank span ahead of a
playhead, a flat centre line, the base map showing through — never as a placeholder
pattern and never as an animation of failing.

Two coverage columns are printed and they are not the same claim: `eye marks` counts
verified files under the component's search seed, and `spec rows` counts verified
files `SPECS-FOR.json` quotes as evidence about that component. `SPECS-FOR.json`
also quotes files for the eleven mark KINDS (`level`, `trace`, `arrive`), which are
not rows of this table and are counted below instead.

| component | reference seed | why that reference | files | eye marks (seed) | spec rows (this component) | median loop (verified clips) | status mix |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `admission` | none — quoted from `motion-tracker` | a balance beam of offered against taken; nothing held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `atField` | none — quoted from `direct-clips` | a written scope drawn as a field boundary; no reference held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `bypass` | none | a bypass taken; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `ceremony` | none | an acceptance ceremony state machine; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `channel` | none | a channel's own state; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `chipBudget` | none | a channel budget in HUD units; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `city` | none — quoted from `rig` | placement as city blocks; closest is a map surface, not held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `collar` | spinner-console | a baseline that breathes with the subject, drawn as a measurement | 11 | 1 seen, 3 refuted | 0 | stills only | 7 reference / 4 look-alike / 0 drift |
| `contextBurn` | none | a creep toward an edge; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `coverage` | motion-tracker — quoted from `direct-clips` | swept ground: observed territory versus ground nobody looked at | 25 | 1 seen, 8 refuted | **1** | stills only | 12 reference / 4 look-alike / 9 drift |
| `dispatch` | fake-os-loaders | a boot sequence: stages entered in order, not a spinner | 19 | 4 seen, 8 refuted | 0 | stills only | 7 reference / 4 look-alike / 8 drift |
| `dominator` | lumon | graded authority in a corporate interface, deliberately inert | 21 | 0 seen, 9 refuted | 0 | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `dossier` | tron-disc — quoted from `cyberpunk-hud` | the identity disc itself | 23 | 0 seen, 4 refuted | **1** | no verified clip | 19 reference / 4 look-alike / 0 drift |
| `envelope` | none — quoted from `direct-clips` | a demand envelope; nothing in the vault draws envelopes | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `esperDive` | none — quoted from `direct-clips` | evidence dive levels; nothing held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `garage` | none | assembly stages as a loadout; no reference held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `gauge` | rig | a suit-integrity spine that drains as integrity is spent | 25 | 1 seen, 11 refuted | 0 | no moving reference | 15 reference / 4 look-alike / 6 drift |
| `gevulot` | none | visibility contract; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `glassCell` | lumon | reviewed through glass, one direction only | 21 | 0 seen, 9 refuted | 0 | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `globe` | hologlobe | a model that turns on its own clock with plotted routes that hold | 19 | 4 seen, 0 refuted | **3** | stills only | 15 reference / 4 look-alike / 0 drift |
| `grid` | none — quoted from `rig` | overview grid doctrine; no reference held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `hardCut` | fake-os-loaders — quoted from `direct-clips` | a progress idiom that changes when the system changes state | 19 | 4 seen, 8 refuted | **2** | stills only | 7 reference / 4 look-alike / 8 drift |
| `ice` | rig | walls as discrete barriers; a breach changes the shape, not the mood | 25 | 1 seen, 11 refuted | 0 | no moving reference | 15 reference / 4 look-alike / 6 drift |
| `individuation` | rig | identity marks on a shared harness, one per unit | 25 | 1 seen, 11 refuted | 0 | no moving reference | 15 reference / 4 look-alike / 6 drift |
| `joiOverlay` | spinner-console | a projection over the observation rather than onto it | 11 | 1 seen, 3 refuted | 0 | stills only | 7 reference / 4 look-alike / 0 drift |
| `keycard` | lumon | an access affordance that is inert until taken | 21 | 0 seen, 9 refuted | 0 | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `killmail` | none — quoted from `direct-clips` | a receipt for a loss; nothing held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `ladder` | lumon | the same authority ladder in another Lumon surface | 21 | 0 seen, 9 refuted | 0 | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `loopDeviation` | none | a loop spent against its period; the fake-OS loaders are nearest, not joined | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `magi` | magi | three cores deliberating, verdict after the vote | 34 | 0 seen, 20 refuted | 0 | no verified clip | 15 reference / 11 look-alike / 8 drift |
| `mfd` | spinner-console — quoted from `direct-clips` | instrument panels whose readouts resolve under a scanning pass | 11 | 1 seen, 3 refuted | **1** | stills only | 7 reference / 4 look-alike / 0 drift |
| `muthur` | lumon — quoted from `direct-clips` | a terminal that answers what it can and prints the refusal | 21 | 0 seen, 9 refuted | **1** | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `needleField` | rig | a spine of segment LEDs, each one a discrete measured state | 25 | 1 seen, 11 refuted | 0 | no moving reference | 15 reference / 4 look-alike / 6 drift |
| `oracle` | magi | fragments of testimony: each one arrives and holds | 34 | 0 seen, 20 refuted | 0 | no verified clip | 15 reference / 11 look-alike / 8 drift |
| `oscillation` | none — quoted from `direct-clips` | a rhythm hunt; sakugabooru-style frame sets would show a period, not held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `queueState` | fake-os-loaders — quoted from `direct-clips` | queued/active states in a fake OS: which state animates and which waits | 19 | 4 seen, 8 refuted | **1** | stills only | 7 reference / 4 look-alike / 8 drift |
| `radar` | motion-tracker — quoted from `direct-clips`, `rig` | the M513-3 sweep: a contact firms as the sweep crosses it and decays | 25 | 1 seen, 8 refuted | **2** | stills only | 12 reference / 4 look-alike / 9 drift |
| `redaction` | tron-disc | an identity withheld, drawn as a shape | 23 | 0 seen, 4 refuted | 0 | no verified clip | 19 reference / 4 look-alike / 0 drift |
| `river` | none — quoted from `direct-clips` | attempt ticks on an axis; the fake-OS boot screens are the nearest idiom, not joined | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `scaleCrush` | none | a fleet wall shrinking; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `scanOverlay` | cyberpunk-hud | a scan pass that resolves what it passes over | 8 | 2 seen, 6 refuted | 0 | no moving reference | 0 reference / 0 look-alike / 8 drift — no usable reference |
| `standardSheet` | none | a legend is a glyph table; the vault holds no reference for a legend refusing | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `stockFlow` | none — quoted from `direct-clips` | a flow that is named but has no stream mark; nothing held | 0 | 0 seen, 0 refuted | **1** | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `strands` | none | delivery routes; closest is a shipping HMI, not held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `stripChart` | spinner-console — quoted from `direct-clips` | a test over time: the line is the reading, the clock is real | 11 | 1 seen, 3 refuted | **1** | stills only | 7 reference / 4 look-alike / 0 drift |
| `syncRatio` | insertion-sync | a percentage climbing under measurement with warning hatching | 25 | 0 seen, 12 refuted | 0 | no verified clip | 17 reference / 4 look-alike / 4 drift |
| `tape` | lumon — quoted from `direct-clips` | nothing moves until the operator moves; a hold is a hold | 21 | 0 seen, 9 refuted | **3** | no verified clip | 21 reference / 0 look-alike / 0 drift |
| `tapeSplice` | none | a splice in a retained reel: nothing in the vault draws an edit point | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |
| `tracker` | motion-tracker — quoted from `direct-clips` | same idiom, second instrument | 25 | 1 seen, 8 refuted | **1** | stills only | 12 reference / 4 look-alike / 9 drift |
| `triVision` | cyberpunk-hud | discrete optic modes, switched not transitioned | 8 | 2 seen, 6 refuted | 0 | no moving reference | 0 reference / 0 look-alike / 8 drift — no usable reference |
| `twoState` | none | commit versus draft; nothing held | 0 | 0 seen, 0 refuted | 0 | no moving reference | **0 reference / 0 look-alike / 0 drift** |

**Spec coverage of the components: 21 of 51** have at least one verified file quoted for them (`admission`, `atField`, `city`, `coverage`, `dossier`, `envelope`, `esperDive`, `globe`, `grid`, `hardCut`, `killmail`, `mfd`, `muthur`, `oscillation`, `queueState`, `radar`, `river`, `stockFlow`, `stripChart`, `tape`, `tracker`). The other 30 are drawn against nothing that survived the eye, and no amount of re-ranking of the current haul changes that — the remaining moving files in `vault/RANK.json` come from the same population that has been rejecting at about one interface in ten. Acquisition, not ranking, is the lever.

Mark kinds quoted: `arrive`, `decay`, `elapsed`, `level`, `rules page`, `trace`.


## What the references do, read off frames

Written against `python3 vault/sheet.py VERIFIED=1 FRAMES=8` — eight samples across
each file's own timeline. `map.py` refuses to write this file while a quoted file has
no reading in `vault/READINGS.json`, so a citation cannot exist without someone having
looked at it. Frame facts are the eye's; durations and travel fractions are measured
by `vault/spec.py` and named as measured.

### `admission` — a balance beam of offered against taken; nothing held

*`motion-tracker--blob.gifcities.org_gifcities_25G2DJC3DOO6R72EAGURMGNK5YW7FRZC.gif`*
- **enters** — a figure standing on a plank, both present on frame zero.
- **moves** — the plank tips and the figure corrects — arms out, one leg lifting off — front-loaded: measured 0.93 of the way by half duration, and after that it holds.
- **holds** — the figure's body and the plank's material; the correction is the only event.
- **period** — 8 frames over 8s — one second per sample, so the file is a held pose with an approach.
- **how it dies** — none visible: the beam does not fall and does not reset.
- **what it asks** — admission draws exactly this: a limit line reached fast and then held. It must not ease in, because the interesting half of a tilt is that it stops.

### `arrive` — a mark kind rather than a specimen

*`spinner-console--blob.gifcities.org_gifcities_2EABIEZLK7C7RZMCFOOHZKIPRAPVWALH.gif`*
- **enters** — a ship on black at the left, already present.
- **moves** — a white streak grows behind the ship as it crosses — measured 1.0 of the way by half duration, so the crossing is over early — and once the ship is gone the streak remains, then a small red and blue burst is all that is left.
- **holds** — the black field. Nothing else is in the frame to hold.
- **period** — 14 frames over 5.22s.
- **how it dies** — the last sample is a burst on black: the event that arrived, reduced to its residue.
- **what it asks** — a trace that reaches its far end early and then holds is a thing that arrived. One that keeps moving has not, and what stays after the head is gone is the evidence — which is why the drawn route, not the moving head, is the part the eye keeps.

### `atField` — a written scope drawn as a field boundary; no reference held

*`solari-departure-flap.gif`*
- **enters** — a board already full. Every row of destinations and times is present on frame zero; the board is not waiting to be populated.
- **moves** — the right-hand column cycles digits one cell at a time — a half-second later the same row shows a different time — and the flap lands with no easing. Rows that are not changing show their faces and hold.
- **holds** — the board's frame, the header, the airline roundels and every row not being updated.
- **period** — 32 frames over 4s. The change is discrete events on a few cells, not a continuous property of the picture.
- **how it dies** — a row nobody has dispatched still shows a value. A flap board has no blank state to hide behind, which is the argument this board settles: an unchanged row holds rather than shimmering.
- **what it asks** — a change arrives by flipping to one of a fixed set of faces, one cell at a time, and everything not involved in the change holds completely.

### `city` — placement as city blocks; closest is a map surface, not held

*`rig--blob.gifcities.org_gifcities_2RO5SN3IFIDBUAFOOVBDCFJ7J6V4DRXR.gif`*
- **enters** — the survey grid: an orange cross-hatch grid with coordinate readouts pinned in the corners and a scattered starfield.
- **moves** — one magenta contact — a block of lit cells — changes position across the field, and the two corner readouts change with it.
- **holds** — the grid lines, the grid's intersection markers, the corner labels' boxes and every star that is not the contact.
- **period** — 15 frames over 3s.
- **how it dies** — absent ground is the grid with nothing on it. The furniture stays, and that is what makes the one contact legible as a contact.
- **what it asks** — the furniture-versus-contact split, stated once and for all: exactly one thing changes, it changes because a measurement changed, and everything that only tells the operator where they are holds perfectly still.

### `coverage` — swept ground: observed territory versus ground nobody looked at

*`hurricane-irma-radar-loop.gif`*
- **enters** — everything except the weather. The coastline, state borders, the colour legend column at the right and the timestamp are all present on frame zero.
- **moves** — the storm complex advances west to east and its cells change intensity through the legend's colours; the timestamp advances with it; the last sample is denser than the first in a way that is not the same pixels shifted.
- **holds** — the map furniture and the legend hold absolutely. Only field data and the clock change.
- **period** — measured 25 frames over 4.17s with no return to frame zero: the window rolls forward and the wrap back to the oldest step is where the loop is, not a rewind of the frame.
- **how it dies** — ground with no return is simply pale and empty. Absence is drawn as the base map showing through, not as a placeholder pattern.
- **what it asks** — a rolling loop: the newest step is drawn, the oldest leaves, and the frame never returns to its first image. Observed ground versus ground nobody looked at must be two different inks over the same fixed map.

### `decay` — a mark kind rather than a specimen

*`scope-envelope-violin.gif`*
- **enters** — an empty lane with a playhead at the left edge. The axis, the lane boundary and the ruler are drawn before any signal exists.
- **moves** — the playhead travels left to right at a constant rate and the waveform is inked behind it, so the past is drawn and the future is blank. Two lanes decay at different rates; the peak above the centre line mirrors the one below.
- **holds** — the ruler, the lane geometry and the horizontal scale hold. The grey unrendered region ahead of the head is not a spinner: it is a statement that nothing has been read there yet.
- **period** — 48 frames over 8s, no return to frame zero. The head covers the frame once, so the file has a duration, not a loop.
- **how it dies** — silence is a flat centre line with no thickness, and the region ahead of the head is the same nothing a specimen draws for an unread span.
- **what it asks** — the measurement is already drawn across the full axis and motion is a position reading over it. A chart that redraws itself is lying about having been recorded.

### `dossier` — the identity disc itself

*`cyberpunk-hud--media.tenor.com_gdhPO38XjlUAAAAM_urith-set.gif.gif`*
- **enters** — a full-frame isometric battlefield with a squad already under control: nameplate bars over every figure and a green selection ring under the chosen one.
- **moves** — the party panel at the right opens late — present in the last two of eight samples across 53 frames, so it arrives at roughly three quarters of the way through — as a grid of portrait cells. The ring stays on its unit and the plates stay over their heads.
- **holds** — the ground, the ring, the nameplates and the panel's frame.
- **period** — 53 frames over 5.3s.
- **how it dies** — a unit with no data still gets a plate. The panel is either open or absent — there is no half-open state.
- **what it asks** — identity furniture holds while one entity is chosen, and a panel arrives as an event with a place to arrive at. Both readings come off this one file: dossier takes the identity ring, the rules page takes the plate that stays put.

### `elapsed` — a mark kind rather than a specimen

*`f16-hud-gcas.gif`*
- **enters** — nothing enters. The whole instrument is drawn on frame zero: airspeed tape left, altitude tape right, heading tape along the bottom, waterline and boresight in the middle. What changes is the ink inside the frame, not the frame.
- **moves** — the three tapes scroll continuously under fixed pointers, so a value is read where the tape passes the pointer, never where the tape is. The numbers beside the pointers step as the tape index advances.
- **holds** — the symbology frame: pointers, waterline, boresight, gun cross, and the labels under each tape hold in place across all 96 frames.
- **period** — measured 96 frames over 16s, no return to frame zero. The tapes are the only continuous thing in it.
- **how it dies** — FLYUP appears in green when measured closure violates the limit and is gone the moment the violation ends. The warning is a state, not an animation: it has no entrance and no exit, which is what makes it legible as a limit rather than as decoration.
- **what it asks** — a limit that acts is drawn in the same ink and at the same place as the thing it limits, held exactly as long as the violation lasts, and the reading surface keeps its geometry while its values move.

### `envelope` — a demand envelope; nothing in the vault draws envelopes

*`f16-hud-gcas.gif`*
- **enters** — nothing enters. The whole instrument is drawn on frame zero: airspeed tape left, altitude tape right, heading tape along the bottom, waterline and boresight in the middle. What changes is the ink inside the frame, not the frame.
- **moves** — the three tapes scroll continuously under fixed pointers, so a value is read where the tape passes the pointer, never where the tape is. The numbers beside the pointers step as the tape index advances.
- **holds** — the symbology frame: pointers, waterline, boresight, gun cross, and the labels under each tape hold in place across all 96 frames.
- **period** — measured 96 frames over 16s, no return to frame zero. The tapes are the only continuous thing in it.
- **how it dies** — FLYUP appears in green when measured closure violates the limit and is gone the moment the violation ends. The warning is a state, not an animation: it has no entrance and no exit, which is what makes it legible as a limit rather than as decoration.
- **what it asks** — a limit that acts is drawn in the same ink and at the same place as the thing it limits, held exactly as long as the violation lasts, and the reading surface keeps its geometry while its values move.

### `esperDive` — evidence dive levels; nothing held

*`solari-departure-flap.gif`*
- **enters** — a board already full. Every row of destinations and times is present on frame zero; the board is not waiting to be populated.
- **moves** — the right-hand column cycles digits one cell at a time — a half-second later the same row shows a different time — and the flap lands with no easing. Rows that are not changing show their faces and hold.
- **holds** — the board's frame, the header, the airline roundels and every row not being updated.
- **period** — 32 frames over 4s. The change is discrete events on a few cells, not a continuous property of the picture.
- **how it dies** — a row nobody has dispatched still shows a value. A flap board has no blank state to hide behind, which is the argument this board settles: an unchanged row holds rather than shimmering.
- **what it asks** — a change arrives by flipping to one of a fixed set of faces, one cell at a time, and everything not involved in the change holds completely.

### `globe` — a model that turns on its own clock with plotted routes that hold

*`hologlobe--blob.gifcities.org_gifcities_2442E47OQDW3JQT5LC2S3S2O6QNUVCEW.gif`*
- **enters** — a pixel globe, complete, on a coloured page.
- **moves** — the page colour behind the globe changes every sample — cream, lavender, green, cyan, blue, pink, orange — while the globe's own rotation is barely measurable.
- **holds** — the globe's silhouette and its continent pattern, effectively entirely.
- **period** — 131 frames over 8s.
- **how it dies** — none visible in the file: it never stops turning and never empties, because the only thing that varies is the page colour behind it. There is no state in which this display has nothing to say, which is the whole reason it is held as a caution rather than as a model.
- **what it asks** — the caution held for the globe: measured travel is nil, so all of this file's apparent liveliness is a hue cycle. A distinction carried only by colour is gone in monochrome, and it was never a measurement either.

*`hologlobe--blob.gifcities.org_gifcities_2BDK66BWLK42ADEG52TMSEUEDO6IZDTX.gif`*
- **enters** — a rendered Earth, already turning, already lit.
- **moves** — the surface advances across the disc — measured travel 0.367 of the frame crossed, 0.43 of the way along at half the duration, which is what the tool records as a constant rate: no acceleration into the turn and no easing out of it. The measurement also calls it not monotone ("it comes back"), and on a rotating sphere that is read as the surface wrapping round the limb and returning to the estimator's brightest region, not as the spin reversing — an interpretation, written here as one.
- **holds** — the sphere's edge, the terminator's shape and the blue field.
- **period** — measured 21 frames over 4.2s at a median delay of 200.0ms, no return to frame zero inside the window.
- **how it dies** — none visible: a globe that stops is a different object and this file never shows one.
- **what it asks** — the rate is the whole spec. A hologlobe that accelerates is a graphic; one that holds a constant rate is an instrument, and the library's 4.00s per turn has to survive being measured the same way.

*`hologlobe--blob.gifcities.org_gifcities_2CW2JW7CEZP4FXCUBBNI2EPWDDLXQQOI.gif`*
- **enters** — a monochrome wireframe globe with a graticule over landmass shapes. No pins, no labels, no callouts.
- **moves** — constant rotation. The mesh and the land under it turn together.
- **holds** — the outer circle and the projection's outline.
- **period** — 12 frames over 3s, no return to frame zero in the sampled window.
- **how it dies** — none visible: the mesh turns for all twelve samples with no state where the rotation stops or the ground goes empty. A stopped globe is a different object and nobody has filmed one, which is why the library's own refusal has to be legible as stillness rather than as a paused frame.
- **what it asks** — the rotation carries the entire claim here, with no plotted furniture to share the work — which is the argument for why the app's pin painting has to keep working on its own meridian when the runtime is switched off.

### `grid` — overview grid doctrine; no reference held

*`rig--blob.gifcities.org_gifcities_2RO5SN3IFIDBUAFOOVBDCFJ7J6V4DRXR.gif`*
- **enters** — the survey grid: an orange cross-hatch grid with coordinate readouts pinned in the corners and a scattered starfield.
- **moves** — one magenta contact — a block of lit cells — changes position across the field, and the two corner readouts change with it.
- **holds** — the grid lines, the grid's intersection markers, the corner labels' boxes and every star that is not the contact.
- **period** — 15 frames over 3s.
- **how it dies** — absent ground is the grid with nothing on it. The furniture stays, and that is what makes the one contact legible as a contact.
- **what it asks** — the furniture-versus-contact split, stated once and for all: exactly one thing changes, it changes because a measurement changed, and everything that only tells the operator where they are holds perfectly still.

### `hardCut` — a progress idiom that changes when the system changes state

*`barcode-scan-receipt.gif`*
- **enters** — the physical world first: a hand, a phone, a barcode on a shelf edge. The app card is not present for the first third of the file.
- **moves** — the reticle holds on the barcode while the hand drifts, then a white card arrives carrying a green confirmation band, then the card's content becomes a receipt with lines and a total.
- **holds** — the viewfinder framing and the reticle hold through the whole wait. Nothing pulses while the operator aims.
- **period** — 24 frames over 4s, no return to frame zero. The wait is roughly two thirds of it, which is the honest ratio of a scan: most of the time is the person, not the software.
- **how it dies** — the card leaves and the frame is the camera again. An unfound scan is shown by the card never arriving, not by a failure animation.
- **what it asks** — an arrival is a thing appearing where nothing was, with its content already correct. The wait before it is allowed to be long and inert, and the confirmation is a state on a card, not a transition.

*`solari-departure-flap.gif`*
- **enters** — a board already full. Every row of destinations and times is present on frame zero; the board is not waiting to be populated.
- **moves** — the right-hand column cycles digits one cell at a time — a half-second later the same row shows a different time — and the flap lands with no easing. Rows that are not changing show their faces and hold.
- **holds** — the board's frame, the header, the airline roundels and every row not being updated.
- **period** — 32 frames over 4s. The change is discrete events on a few cells, not a continuous property of the picture.
- **how it dies** — a row nobody has dispatched still shows a value. A flap board has no blank state to hide behind, which is the argument this board settles: an unchanged row holds rather than shimmering.
- **what it asks** — a change arrives by flipping to one of a fixed set of faces, one cell at a time, and everything not involved in the change holds completely.

### `killmail` — a receipt for a loss; nothing held

*`barcode-scan-receipt.gif`*
- **enters** — the physical world first: a hand, a phone, a barcode on a shelf edge. The app card is not present for the first third of the file.
- **moves** — the reticle holds on the barcode while the hand drifts, then a white card arrives carrying a green confirmation band, then the card's content becomes a receipt with lines and a total.
- **holds** — the viewfinder framing and the reticle hold through the whole wait. Nothing pulses while the operator aims.
- **period** — 24 frames over 4s, no return to frame zero. The wait is roughly two thirds of it, which is the honest ratio of a scan: most of the time is the person, not the software.
- **how it dies** — the card leaves and the frame is the camera again. An unfound scan is shown by the card never arriving, not by a failure animation.
- **what it asks** — an arrival is a thing appearing where nothing was, with its content already correct. The wait before it is allowed to be long and inert, and the confirmation is a state on a card, not a transition.

### `level` — a mark kind rather than a specimen

*`fake-os-loaders--blob.gifcities.org_gifcities_36SZVJ74JXC6QPNLGBGDNMW26HNU7TFW.gif`*
- **enters** — a named lane with a machine icon at each end and the bar empty between them. Both ends are present on frame zero.
- **moves** — a green head crosses the lane at a constant rate — measured 0.70 of the frame travelled, 0.46 of the way at half duration, so the rate is even and the head does not ease — and the filled region behind it is the same green, same thickness.
- **holds** — both end icons and the lane itself. Four of twelve sampled grid cells never move at all.
- **period** — 49 frames, no delay recorded in the file, so the period is the player's, not the artist's.
- **how it dies** — an empty lane is the lane with no ink in it. The track is drawn before there is anything on it, which is what a zero and an unknown must not be allowed to share.
- **what it asks** — extent is the measurement and the ground is furniture: a bar reads as a quantity because the ends and the track never move while the head does, at a rate a viewer can check against the distance.

### `mfd` — instrument panels whose readouts resolve under a scanning pass

*`f16-hud-gcas.gif`*
- **enters** — nothing enters. The whole instrument is drawn on frame zero: airspeed tape left, altitude tape right, heading tape along the bottom, waterline and boresight in the middle. What changes is the ink inside the frame, not the frame.
- **moves** — the three tapes scroll continuously under fixed pointers, so a value is read where the tape passes the pointer, never where the tape is. The numbers beside the pointers step as the tape index advances.
- **holds** — the symbology frame: pointers, waterline, boresight, gun cross, and the labels under each tape hold in place across all 96 frames.
- **period** — measured 96 frames over 16s, no return to frame zero. The tapes are the only continuous thing in it.
- **how it dies** — FLYUP appears in green when measured closure violates the limit and is gone the moment the violation ends. The warning is a state, not an animation: it has no entrance and no exit, which is what makes it legible as a limit rather than as decoration.
- **what it asks** — a limit that acts is drawn in the same ink and at the same place as the thing it limits, held exactly as long as the violation lasts, and the reading surface keeps its geometry while its values move.

### `muthur` — a terminal that answers what it can and prints the refusal

*`sweep-hearing-range.gif`*
- **enters** — a slow single hump on a black field. No axis, no labels, no furniture at all.
- **moves** — frequency climbs monotonically — cycles per width rise step by step across the file — while amplitude ramps up behind it, so the picture gets both denser and taller.
- **holds** — nothing. This is the only verified file in the vault with no fixed ink in it at all, which is why it is quoted for exactly one claim.
- **period** — 50 frames over 10s, no return to frame zero.
- **how it dies** — the last sample is a flat line at the centre with the oscillation gone. A sweep that has run out of range is drawn as a line, not as an error.
- **what it asks** — a rate may change because a measured frequency changed, and the amplitude may follow it. Nothing else may move, and the end of the range is a flat line rather than a fade.

### `oscillation` — a rhythm hunt; sakugabooru-style frame sets would show a period, not held

*`sweep-hearing-range.gif`*
- **enters** — a slow single hump on a black field. No axis, no labels, no furniture at all.
- **moves** — frequency climbs monotonically — cycles per width rise step by step across the file — while amplitude ramps up behind it, so the picture gets both denser and taller.
- **holds** — nothing. This is the only verified file in the vault with no fixed ink in it at all, which is why it is quoted for exactly one claim.
- **period** — 50 frames over 10s, no return to frame zero.
- **how it dies** — the last sample is a flat line at the centre with the oscillation gone. A sweep that has run out of range is drawn as a line, not as an error.
- **what it asks** — a rate may change because a measured frequency changed, and the amplitude may follow it. Nothing else may move, and the end of the range is a flat line rather than a fade.

### `queueState` — queued/active states in a fake OS: which state animates and which waits

*`solari-departure-flap.gif`*
- **enters** — a board already full. Every row of destinations and times is present on frame zero; the board is not waiting to be populated.
- **moves** — the right-hand column cycles digits one cell at a time — a half-second later the same row shows a different time — and the flap lands with no easing. Rows that are not changing show their faces and hold.
- **holds** — the board's frame, the header, the airline roundels and every row not being updated.
- **period** — 32 frames over 4s. The change is discrete events on a few cells, not a continuous property of the picture.
- **how it dies** — a row nobody has dispatched still shows a value. A flap board has no blank state to hide behind, which is the argument this board settles: an unchanged row holds rather than shimmering.
- **what it asks** — a change arrives by flipping to one of a fixed set of faces, one cell at a time, and everything not involved in the change holds completely.

### `radar` — the M513-3 sweep: a contact firms as the sweep crosses it and decays

*`hurricane-irma-radar-loop.gif`*
- **enters** — everything except the weather. The coastline, state borders, the colour legend column at the right and the timestamp are all present on frame zero.
- **moves** — the storm complex advances west to east and its cells change intensity through the legend's colours; the timestamp advances with it; the last sample is denser than the first in a way that is not the same pixels shifted.
- **holds** — the map furniture and the legend hold absolutely. Only field data and the clock change.
- **period** — measured 25 frames over 4.17s with no return to frame zero: the window rolls forward and the wrap back to the oldest step is where the loop is, not a rewind of the frame.
- **how it dies** — ground with no return is simply pale and empty. Absence is drawn as the base map showing through, not as a placeholder pattern.
- **what it asks** — a rolling loop: the newest step is drawn, the oldest leaves, and the frame never returns to its first image. Observed ground versus ground nobody looked at must be two different inks over the same fixed map.

*`rig--blob.gifcities.org_gifcities_2RO5SN3IFIDBUAFOOVBDCFJ7J6V4DRXR.gif`*
- **enters** — the survey grid: an orange cross-hatch grid with coordinate readouts pinned in the corners and a scattered starfield.
- **moves** — one magenta contact — a block of lit cells — changes position across the field, and the two corner readouts change with it.
- **holds** — the grid lines, the grid's intersection markers, the corner labels' boxes and every star that is not the contact.
- **period** — 15 frames over 3s.
- **how it dies** — absent ground is the grid with nothing on it. The furniture stays, and that is what makes the one contact legible as a contact.
- **what it asks** — the furniture-versus-contact split, stated once and for all: exactly one thing changes, it changes because a measurement changed, and everything that only tells the operator where they are holds perfectly still.

### `river` — attempt ticks on an axis; the fake-OS boot screens are the nearest idiom, not joined

*`scope-envelope-violin.gif`*
- **enters** — an empty lane with a playhead at the left edge. The axis, the lane boundary and the ruler are drawn before any signal exists.
- **moves** — the playhead travels left to right at a constant rate and the waveform is inked behind it, so the past is drawn and the future is blank. Two lanes decay at different rates; the peak above the centre line mirrors the one below.
- **holds** — the ruler, the lane geometry and the horizontal scale hold. The grey unrendered region ahead of the head is not a spinner: it is a statement that nothing has been read there yet.
- **period** — 48 frames over 8s, no return to frame zero. The head covers the frame once, so the file has a duration, not a loop.
- **how it dies** — silence is a flat centre line with no thickness, and the region ahead of the head is the same nothing a specimen draws for an unread span.
- **what it asks** — the measurement is already drawn across the full axis and motion is a position reading over it. A chart that redraws itself is lying about having been recorded.

### `rules page` — a mark kind rather than a specimen

*`cyberpunk-hud--media.tenor.com_gdhPO38XjlUAAAAM_urith-set.gif.gif`*
- **enters** — a full-frame isometric battlefield with a squad already under control: nameplate bars over every figure and a green selection ring under the chosen one.
- **moves** — the party panel at the right opens late — present in the last two of eight samples across 53 frames, so it arrives at roughly three quarters of the way through — as a grid of portrait cells. The ring stays on its unit and the plates stay over their heads.
- **holds** — the ground, the ring, the nameplates and the panel's frame.
- **period** — 53 frames over 5.3s.
- **how it dies** — a unit with no data still gets a plate. The panel is either open or absent — there is no half-open state.
- **what it asks** — identity furniture holds while one entity is chosen, and a panel arrives as an event with a place to arrive at. Both readings come off this one file: dossier takes the identity ring, the rules page takes the plate that stays put.

*`fake-os-loaders--blob.gifcities.org_gifcities_2S4N3JQ6GIQGIKRNQ6CX4AAHKS6DQHE6.gif`*
- **enters** — the word LOADING in blue with a yellow disc above it on white. Nothing else ever enters.
- **moves** — the word rotates through a full turn while the disc orbits it, and the word's hue walks blue to violet to magenta on the cycle.
- **holds** — the white field. There is no ground, no axis, no value.
- **period** — 30 frames over 2.4s, a clean loop.
- **how it dies** — none visible: nothing in this file ever stops, which is the defect. A display with nothing to say has no way to say it here.
- **what it asks** — counter-example. Everything moving on a display with one quantity, and that quantity is the dot count, is the idiom this library refuses.

*`fake-os-loaders--blob.gifcities.org_gifcities_34C5WYILX3AL74KKP36H6JB75QTVKCUD.gif`*
- **enters** — the word loading in a blackletter face on black. Present on frame zero.
- **moves** — an ellipsis after the word grows to one dot, two, three, then shrinks back. The word itself never moves.
- **holds** — the wordmark and its position, completely, for every frame.
- **period** — 6 frames over 0.24s of file delay — the fastest loop in the vault, and the shortest thing anyone thought worth animating.
- **how it dies** — none visible. The dots are the whole output.
- **what it asks** — counter-example, and the sharpest one: three glyphs cycling is the entire motion vocabulary of an era that had nothing to measure, quoted beside the rule that says motion is a measurement.

### `stockFlow` — a flow that is named but has no stream mark; nothing held

*`solari-departure-flap.gif`*
- **enters** — a board already full. Every row of destinations and times is present on frame zero; the board is not waiting to be populated.
- **moves** — the right-hand column cycles digits one cell at a time — a half-second later the same row shows a different time — and the flap lands with no easing. Rows that are not changing show their faces and hold.
- **holds** — the board's frame, the header, the airline roundels and every row not being updated.
- **period** — 32 frames over 4s. The change is discrete events on a few cells, not a continuous property of the picture.
- **how it dies** — a row nobody has dispatched still shows a value. A flap board has no blank state to hide behind, which is the argument this board settles: an unchanged row holds rather than shimmering.
- **what it asks** — a change arrives by flipping to one of a fixed set of faces, one cell at a time, and everything not involved in the change holds completely.

### `stripChart` — a test over time: the line is the reading, the clock is real

*`scope-envelope-violin.gif`*
- **enters** — an empty lane with a playhead at the left edge. The axis, the lane boundary and the ruler are drawn before any signal exists.
- **moves** — the playhead travels left to right at a constant rate and the waveform is inked behind it, so the past is drawn and the future is blank. Two lanes decay at different rates; the peak above the centre line mirrors the one below.
- **holds** — the ruler, the lane geometry and the horizontal scale hold. The grey unrendered region ahead of the head is not a spinner: it is a statement that nothing has been read there yet.
- **period** — 48 frames over 8s, no return to frame zero. The head covers the frame once, so the file has a duration, not a loop.
- **how it dies** — silence is a flat centre line with no thickness, and the region ahead of the head is the same nothing a specimen draws for an unread span.
- **what it asks** — the measurement is already drawn across the full axis and motion is a position reading over it. A chart that redraws itself is lying about having been recorded.

### `tape` — nothing moves until the operator moves; a hold is a hold

*`barcode-scan-receipt.gif`*
- **enters** — the physical world first: a hand, a phone, a barcode on a shelf edge. The app card is not present for the first third of the file.
- **moves** — the reticle holds on the barcode while the hand drifts, then a white card arrives carrying a green confirmation band, then the card's content becomes a receipt with lines and a total.
- **holds** — the viewfinder framing and the reticle hold through the whole wait. Nothing pulses while the operator aims.
- **period** — 24 frames over 4s, no return to frame zero. The wait is roughly two thirds of it, which is the honest ratio of a scan: most of the time is the person, not the software.
- **how it dies** — the card leaves and the frame is the camera again. An unfound scan is shown by the card never arriving, not by a failure animation.
- **what it asks** — an arrival is a thing appearing where nothing was, with its content already correct. The wait before it is allowed to be long and inert, and the confirmation is a state on a card, not a transition.

*`f16-hud-gcas.gif`*
- **enters** — nothing enters. The whole instrument is drawn on frame zero: airspeed tape left, altitude tape right, heading tape along the bottom, waterline and boresight in the middle. What changes is the ink inside the frame, not the frame.
- **moves** — the three tapes scroll continuously under fixed pointers, so a value is read where the tape passes the pointer, never where the tape is. The numbers beside the pointers step as the tape index advances.
- **holds** — the symbology frame: pointers, waterline, boresight, gun cross, and the labels under each tape hold in place across all 96 frames.
- **period** — measured 96 frames over 16s, no return to frame zero. The tapes are the only continuous thing in it.
- **how it dies** — FLYUP appears in green when measured closure violates the limit and is gone the moment the violation ends. The warning is a state, not an animation: it has no entrance and no exit, which is what makes it legible as a limit rather than as decoration.
- **what it asks** — a limit that acts is drawn in the same ink and at the same place as the thing it limits, held exactly as long as the violation lasts, and the reading surface keeps its geometry while its values move.

*`scope-envelope-violin.gif`*
- **enters** — an empty lane with a playhead at the left edge. The axis, the lane boundary and the ruler are drawn before any signal exists.
- **moves** — the playhead travels left to right at a constant rate and the waveform is inked behind it, so the past is drawn and the future is blank. Two lanes decay at different rates; the peak above the centre line mirrors the one below.
- **holds** — the ruler, the lane geometry and the horizontal scale hold. The grey unrendered region ahead of the head is not a spinner: it is a statement that nothing has been read there yet.
- **period** — 48 frames over 8s, no return to frame zero. The head covers the frame once, so the file has a duration, not a loop.
- **how it dies** — silence is a flat centre line with no thickness, and the region ahead of the head is the same nothing a specimen draws for an unread span.
- **what it asks** — the measurement is already drawn across the full axis and motion is a position reading over it. A chart that redraws itself is lying about having been recorded.

### `trace` — a mark kind rather than a specimen

*`fake-os-loaders--blob.gifcities.org_gifcities_36SZVJ74JXC6QPNLGBGDNMW26HNU7TFW.gif`*
- **enters** — a named lane with a machine icon at each end and the bar empty between them. Both ends are present on frame zero.
- **moves** — a green head crosses the lane at a constant rate — measured 0.70 of the frame travelled, 0.46 of the way at half duration, so the rate is even and the head does not ease — and the filled region behind it is the same green, same thickness.
- **holds** — both end icons and the lane itself. Four of twelve sampled grid cells never move at all.
- **period** — 49 frames, no delay recorded in the file, so the period is the player's, not the artist's.
- **how it dies** — an empty lane is the lane with no ink in it. The track is drawn before there is anything on it, which is what a zero and an unknown must not be allowed to share.
- **what it asks** — extent is the measurement and the ground is furniture: a bar reads as a quantity because the ends and the track never move while the head does, at a rate a viewer can check against the distance.

*`spinner-console--blob.gifcities.org_gifcities_2EABIEZLK7C7RZMCFOOHZKIPRAPVWALH.gif`*
- **enters** — a ship on black at the left, already present.
- **moves** — a white streak grows behind the ship as it crosses — measured 1.0 of the way by half duration, so the crossing is over early — and once the ship is gone the streak remains, then a small red and blue burst is all that is left.
- **holds** — the black field. Nothing else is in the frame to hold.
- **period** — 14 frames over 5.22s.
- **how it dies** — the last sample is a burst on black: the event that arrived, reduced to its residue.
- **what it asks** — a trace that reaches its far end early and then holds is a thing that arrived. One that keeps moving has not, and what stays after the head is gone is the evidence — which is why the drawn route, not the moving head, is the part the eye keeps.

### `tracker` — same idiom, second instrument

*`hurricane-irma-radar-loop.gif`*
- **enters** — everything except the weather. The coastline, state borders, the colour legend column at the right and the timestamp are all present on frame zero.
- **moves** — the storm complex advances west to east and its cells change intensity through the legend's colours; the timestamp advances with it; the last sample is denser than the first in a way that is not the same pixels shifted.
- **holds** — the map furniture and the legend hold absolutely. Only field data and the clock change.
- **period** — measured 25 frames over 4.17s with no return to frame zero: the window rolls forward and the wrap back to the oldest step is where the loop is, not a rewind of the frame.
- **how it dies** — ground with no return is simply pale and empty. Absence is drawn as the base map showing through, not as a placeholder pattern.
- **what it asks** — a rolling loop: the newest step is drawn, the oldest leaves, and the frame never returns to its first image. Observed ground versus ground nobody looked at must be two different inks over the same fixed map.


## What this vault cannot show yet

**The prop-accurate files do not move, and the files that move are not the prop.** Of the files marked `reference` — reached through a tag that names the idiom — **130 are single-frame** and only **17 move**; meanwhile **38 of the moving files are `look-alike`**, produced by a host's keyword or semantic search rather than by a page that identifies the object. Article archives (scifiinterfaces) photograph screens; the animation lives in GIF sets hosted where the search is fuzzy. Until the vault holds frames from the named props themselves, any 'duration and easing' claim is an inference from a look-alike, and the table above says which column is which.

**25 of 51 components have no usable reference in the vault:** `admission`, `atField`, `bypass`, `ceremony`, `channel`, `chipBudget`, `city`, `contextBurn`, `envelope`, `esperDive`, `garage`, `gevulot`, `grid`, `killmail`, `loopDeviation`, `oscillation`, `river`, `scaleCrush`, `scanOverlay`, `standardSheet`, `stockFlow`, `strands`, `tapeSplice`, `triVision`, `twoState`.

The shape of that list is the shape of the acquisition: the vault is strong where
the idiom is famous — motion trackers, hologlobe tables, fake operating systems,
Lumon's terminals — and empty where the specimen is a *quantity* the library invented
(a context-burn creep, a chip budget, a fleet-wall scale crush). Those are the ones
a reference set matters least for and hardest to argue from, and they are the honest
boundary of what a gauntlet run today can claim.

### And one finding that outranks the coverage numbers

**A text-derived relevance label says nothing about the picture.** The three largest
moving files under the flagship `motion-tracker` seed carry `work: Aliens` in the
manifest. Their frames are a Dr. Wily cartoon dancing, a man falling over in a
corridor, and a person holding a kitchen fan in front of their face under the caption
*I'M YOUR BIGGEST FAN*. Nothing about `mark.py` is wrong — it read the page, and the
page really did say motion tracker — but a 4.0s loop measured off a fan is not a
statement about interfaces, so every loop and delay number in the table above
describes memes until a person has looked at the frames.

`vault/eyeball.py` writes an eight-frame strip per moving file into a checklist
(`vault/EYEBALL.md`), and the judgement is recorded in `vault/EYEBALL.json` as
`contentVerified`. **Right now 9 of 132 moving files have been looked at, and all
three of the ones actually examined are refuted**, so no moving file in this vault may
be quoted in a motion spec. The route to prop-accurate motion is not more search-engine
GIF hosts: it is sources whose page *and* picture both identify the prop, with an eye on
every file before a number is taken from it.

Vault status counts: 87 drift, 39 look-alike, 147 reference, 8 unmarked.

Re-run after every acquisition and after any change to the component list: `python3 vault/map.py`.
